[mysqld]
server-id=1
log-bin=mysql-bin
mysql_native_password=ON
binlog_expire_logs_seconds=604800

# Optional: Replication options
# binlog-do-db=my_database   # 指定要複製的資料庫，這是可選的
# binlog-ignore-db=mysql   # 忽略某些資料庫
